# CodeBrain 项目演进记录

## 总览

这个项目的演进路线已经比较清晰：先验证一个面向代码仓库的 agentic test loop 原型，再重构为通用的本地仓库智能平台，随后补齐混合检索、仓库问答、多语言分析和闭环验证能力。整体方向，是从“一次性闭环执行”逐步演进到“可持续索引、可解释检索、可复用工作台”。

## 阶段一：AgenticTestLoop 原型期

- 时间：2026-03-18
- 关键提交：`b0440abe` `Initial import of agentic_test_loop`

这一阶段的核心目标，是先把“面向代码仓库的 agentic test loop”跑通。项目名称当时还是 `AgenticTestLoop`，架构重点放在闭环执行链路，而不是长期仓库知识沉淀。

当时已经具备的核心能力包括：

- 基于 Roslyn 的仓库映射与知识图谱抽取；
- 一组工作流 Agent，例如 `RepoMapper`、`Understander`、`DrilldownNavigator`、`TestPlanner`、`TestWriter`、`Runner`、`Coverage`；
- CLI 与本地 API/UI；
- 文件型产物存储、测试执行与覆盖率采集；
- 测试样例生成与基础存储测试。

这一版的本质，是一个围绕“理解代码并生成/执行测试”的验证型系统。它证明了闭环流程可行，但在知识持久化、仓库注册、增量索引与统一检索这些平台能力上仍然偏弱。

## 阶段二：平台化重构期

- 时间：2026-03-20
- 关键提交：`5a02e3e8` `Rename project to CodeBrain and add local repository indexing`

这一阶段是第一次明确的产品定位升级。项目从 `AgenticTestLoop` 更名为 `CodeBrain`，目标从“测试闭环 demo”转向“本地仓库智能中枢”。

关键变化包括：

- 全仓统一改名，结构从 `AgenticTestLoop.*` 迁移到 `CodeBrain.*`；
- 将 Agent 工作流下沉到 `CodeBrain.Workflows`，使其成为平台的一部分而不是项目本体；
- 引入 `SqliteRepositoryCatalog` 和 `SqliteRepositoryIndexStore`，开始具备真正的仓库注册与持久化索引能力；
- 引入 `LocalFileIncrementalIndexPlanner`，支持基于文件变化的增量索引；
- 补充 `RepositoryIndexing` 相关抽象和数据模型；
- 重构并扩展 CLI 与 API，使能力开始围绕“仓库管理 + 索引管理”组织。

这一阶段的意义在于：项目开始从“执行单次闭环任务”转变为“先建立仓库知识底座，再在其上执行工作流”。CodeBrain 的核心开始从 Agent 编排迁移到仓库智能基础设施。

## 阶段三：检索与问答工作台期

- 时间：2026-03-23
- 关键提交：`c93670d` `Add hybrid retrieval and repository QA workspace`

这一阶段把 CodeBrain 从“有索引的平台”推进到“能查询、能解释、能规划修改”的工程工作台形态，是当前版本最关键的一次能力扩展。

主要演进点包括：

- 增加混合检索能力，把词法检索、向量检索和图扩展结合起来，并提供可解释的评分拆解；
- 增加 `RepositoryContextBundle`、`RepositoryChangeScope`、`RepositoryEditPlan` 等模型，让查询结果不再只是命中列表，而是可直接服务于修改规划；
- 引入 `GitRepositoryInspector`，把增量分析从纯文件扫描升级为 Git 感知；
- 增加 `RepositoryLanguageDetector` 与 `RepositoryFileScanner`，使仓库注册、扫描和索引更加稳定统一；
- 新增 `CodeBrain.Analysis.Text`，为非 C# 仓库提供轻量结构化分析后端，说明系统已经开始向多语言可扩展框架演进；
- 大幅增强 API/UI，形成“仓库 QA 工作台”：支持仓库选择、重建索引、自然语言提问、证据卡片、源码片段预览、符号上下文与影响分析；
- 工作流继续前推，新增 `ChangeScope` 和 `EditPlanner` 相关 Agent，让闭环执行和仓库检索真正打通。

这一阶段之后，CodeBrain 的产品形态已经比较明确：它不是单纯的测试生成器，也不是单纯的代码搜索器，而是一个本地仓库智能层，既能沉淀索引，又能支撑问答、影响分析、修改规划和测试闭环。

## 当前架构定位

从当前 `README.md` 和 `ARCHITECTURE.md` 可以看出，项目已经形成三条共享同一套仓库智能模型的主链路：

- 索引链路：注册仓库、选择分析器、执行增量分析、持久化索引；
- 查询链路：混合检索、图扩展、上下文组装、编辑计划生成；
- 闭环链路：理解、钻取、变更范围、编辑规划、测试规划、测试生成、执行、覆盖率验证、记忆提升。

当前代码结构也体现了这一分层：

- `CodeBrain.Core`：核心抽象与模型；
- `CodeBrain.Storage`：SQLite、索引、增量分析与检索支撑；
- `CodeBrain.Analysis.CSharp` / `CodeBrain.Analysis.Text`：分析后端；
- `CodeBrain.Workflows`：闭环工作流 Agent；
- `CodeBrain.Execution`：执行与覆盖率；
- `CodeBrain.Cli` / `CodeBrain.Api`：交互入口。

## 演进主线总结

如果用一句话概括，这个项目的演进过程是：

从“验证 Agent 驱动的测试闭环是否可行”，演进到“构建本地仓库知识底座”，再进一步演进为“以混合检索和仓库问答为核心，并能反向驱动修改与验证的本地工程智能平台”。

更具体地说，演进主线可以归纳为四个关键词：

- `原型化`：先验证闭环工作流与 Roslyn 仓库建模；
- `平台化`：引入统一命名、分层架构、仓库注册与持久化索引；
- `检索化`：把索引转化为可查询、可解释、可扩展的检索系统；
- `工程化`：让查询结果直接进入变更范围分析、编辑规划和测试验证闭环。

## 当前阶段判断

项目目前已经越过“概念验证”阶段，进入“平台能力成型、产品体验持续打磨”的阶段。下一步方向也已经在架构文档和待办中明确体现：

- 将本地向量实现替换为可插拔 embedding provider；
- 持续增强 Git diff 粒度与 staged/unstaged 过滤；
- 将混合检索进一步独立模块化；
- 让测试生成更贴近真实编辑意图，而不是停留在脚手架层；
- 把非 C# 分析器从结构解析升级到更真实的 AST 级实现。

这说明 CodeBrain 当前的重点，已经不是“能不能做”，而是“如何把现有能力做得更准、更稳、更可扩展”。

## 2026-04-04：文档中文化与协作可读性修复

当前仓库中的核心 Markdown 文档仍以英文为主，且 `evolution.md` 存在明显乱码。这会直接影响项目在中文协作场景下的可读性，也削弱待办、完成记录和演进记录的持续维护价值。

本次更新完成了以下演进：

- 将 README、架构说明、验证说明、待办清单、完成记录统一改写为中文；
- 重写并修复 `evolution.md`，消除乱码并恢复结构化演进叙述；
- 保留原有文档的信息结构，同时让文档更适合作为仓库内的长期协作资产。

这次变更没有扩展代码能力，但强化了产品的协作文档层。对于一个强调“人和 AI 一起维护项目”的仓库来说，文档可读性本身就是平台能力的一部分。

## 2026-04-08：embedding 路径从存储细节演进为可替换能力

此前的混合检索虽然已经能运行，但向量生成逻辑直接写死在 `SqliteRepositoryIndexStore` 中。这意味着索引持久化、查询打分和本地 hash 向量实现被耦合在一起，后续若要接入更高质量的 embedding 服务，就必须同时改动存储层、查询层和测试，演进成本过高。

本次更新把这一块能力向前推进了一步：

- 在 `CodeBrain.Core` 中新增 `IEmbeddingProvider` 契约，以及携带 `Model` 和 `Values` 的 `RepositoryEmbedding` 模型；
- 在 `CodeBrain.Storage` 中保留默认离线回退实现 `LocalHashEmbeddingProvider`，确保当前本地工作流不依赖外部服务也能继续运行；
- 索引写入时不再假定固定模型名，而是持久化 provider 返回的模型标识；
- 查询阶段只在查询向量与文档向量模型一致时才计算向量相似度，避免 provider 切换后出现隐式错配；
- 通过注入式测试验证 provider 抽象确实进入了保存与检索主链路，而不是只停留在接口层。

这次演进的价值，不只是“抽了一个接口”，而是把 embedding 从存储实现细节提升为平台能力边界。对 CodeBrain 这样的本地仓库智能平台来说，这意味着后续可以更自然地接入在线 embedding、本地模型、混合回退策略和 provider 级配置，而不用再次拆穿当前索引主链路。

## 2026-04-08：仓库问答界面从结果页演进为仓库管理工作台

此前的浏览器界面已经具备仓库选择、自然语言提问、证据查看和源码预览能力，但整体结构仍然偏向“旧式查询页”：仓库选择、提问表单和结果区被顺序堆叠，左侧仓库管理和右侧工作区之间缺少明确分工，也不足以承载后续的多仓库协作体验。

本次更新把这条产品线继续向“工程工作台”推进了一步：

- 将前端主界面重构为双栏布局，左侧集中管理仓库卡片、索引动作和新增仓库入口，右侧聚焦对话、节点浏览和代码影响预览；
- 新增 `GET /api/workspace` 聚合接口，把仓库卡片所需的分支、文件数、索引状态和图摘要集中整理为前端友好的工作区视图模型；
- 将问答结果从单次答案区改造成会话流表达，让每次提问都能保留在右侧时间线中，并继续关联证据项、建议修改计划和下方源码/上下文/影响面板；
- 保留原有检索与源码预览链路，但把它们重新组织进更符合“仓库管理 + 基于仓库的对话 + 代码检查”的操作路径中。

这次演进的重点不是单纯“换一套样式”，而是让 UI 更接近项目目标中强调的人机协作工作台。用户看到的已经不再只是一个问答框，而是一个可切换仓库、可触发索引、可提问、可追证据、可下钻源码的协作界面。它为后续引入多会话状态、操作回放、验证闭环和更强的仓库导航打下了更自然的产品骨架。
