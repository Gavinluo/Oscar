# 待办清单

## 进行中

- [ ] 增加 Git diff 范围选择，以及 staged/unstaged 过滤。
  目标：让增量索引和变更范围分析更贴近真实开发流程，而不是总是基于整个工作树。
  关注点：
  - 扩展规划器输入模型；
  - 支持 HEAD 范围与工作树模式；
  - 在 CLI 和 API 中暴露这些选项。

## 下一步

- [ ] 为仓库管理工作台补充多会话状态与操作回放。
  目标：让“基于仓库的对话”从单页面交互进一步演进为可切换、可追踪、可复查的工程协作流程。
  关注点：
  - 为不同仓库保留独立会话历史；
  - 记录索引、提问、证据查看等关键操作轨迹；
  - 为后续引入验证清单与执行回放预留数据结构。

- [ ] 将测试脚手架升级为感知编辑意图的测试生成。
  目标：让测试输出不再停留在占位模板，而是能够反映编辑计划、受影响符号和验证目标。
  关注点：
  - 消费 `RepositoryEditPlan`；
  - 改进生成的断言与夹具提示；
  - 为工作流产物补充回归测试。

- [ ] 将混合检索提取为独立搜索模块。
  目标：降低存储层与检索编排之间的耦合，让排序、上下文组装和后续检索实验可以独立演进。
  关注点：
  - 将混合排序与上下文服务迁移到更窄的边界之后；
  - 保持 CLI/API 响应结构稳定；
  - 在拆分过程中保留当前检索测试。

- [ ] 将轻量文本后端替换为面向 AST 的路径。
  目标：让非 C# 仓库理解在真实的混合语言仓库中更有用，而不是只停留在结构化文件解析层面。
  关注点：
  - 选择一门具体语言作为首个深入实现；
  - 梳理符号/引用提取需求；
  - 定义与 C# 路径对齐的检索文档模型。

## 验证基线

- [ ] 在推进路线图的同时保持解决方案测试为绿色。
  当前基线：`dotnet test CodeBrain.sln --no-restore` 在 2026-04-08 通过，结果为 25/25。
