# Agents.md

本文件用于告诉后续参与该仓库工作的 AI：当前项目的目标、主工作目录、设计方向和协作原则。

## 1. 主工作目录

后续工作的主目录是：

- `D:\code\CodeBase\agentic_test_loop`

除非明确说明，不要把主要精力放在其他平行目录。默认应先在 `agentic_test_loop` 中理解代码、修改代码、运行命令、补充文档和推进设计。

## 2. 项目核心目标

这个项目的主要目标，不是做一个通用聊天机器人，而是完成一个能够基于存量 C# 项目的辅助 AI 软件。

这个 AI 软件应当面向真实的本地工程仓库，重点解决以下问题：

- 理解已有 C# 项目的结构、模块、依赖关系和业务语义。
- 帮助人类开发者快速回答关于当前仓库的业务问题和实现问题。
- 帮助定位问题、分析变更影响、缩小排查范围。
- 帮助补充、生成、修复和完善单元测试。
- 帮助形成“理解代码 -> 提出修改建议 -> 验证修改结果”的半自动化工作流。

所以后续任何设计、重构或功能扩展，都应优先服务这个方向：让 AI 更懂存量 C# 项目，并能真正参与维护它。

## 3. 产品设计方向

你必须尽最大努力，把这个项目设计成一个半自动化 AI 产品，而不是零散工具的集合。

“半自动化”意味着：

- AI 不只是被动回答问题，还要主动提供上下文、证据、候选路径和下一步建议。
- AI 不应该一上来就完全替代人，而应与人协作，共同推进任务。
- AI 的输出应尽量可验证、可追踪、可回放，而不是只给模糊结论。
- AI 应尽可能利用仓库索引、代码图谱、检索、日志、测试、覆盖率、变更范围等信息，减少人类重复劳动。

后续设计时，应持续朝这些高价值能力推进：

- 快速回答当前仓库的业务问题、代码问题、调用链问题、影响范围问题。
- 根据错误日志、异常堆栈、运行现象和上下文线索辅助定位 bug。
- 自动或半自动补充单元测试、回归测试和测试脚手架。
- 对变更提供建议性的修改范围、验证清单和测试建议。
- 将用户问题、检索证据、编辑计划、测试结果组织成可持续复用的工程知识。

## 4. 人机协作原则

这个产品的目标不是“让 AI 单独完成一切”，而是“让 AI 更方便地与人一起维护并持续成长这个产品”。

因此你在设计功能时，必须优先考虑以下协作原则：

- 输出必须尽量结构化，让人能快速判断是否可信。
- 所有重要建议，最好都带证据来源、相关文件、符号、调用关系或日志依据。
- 对高风险修改，应提供范围说明、影响分析和验证建议。
- 对自动生成的代码、测试、修复建议，应尽量给出可审阅、可调整的中间结果。
- 应尽量减少“黑盒式自动化”，增强“可介入的自动化”。

你的目标是让人类开发者感觉：这个系统不是一个只会说话的助手，而是一个逐步成长中的工程协作者。

## 5. UI / 交互要求

与人一起维护存量项目，离不开好的交互 UI。你必须持续思考如何进化出更好的交互，而不是只满足于“能用”。

后续在设计 UI 和交互时，至少要考虑这些方向：

- 如何让用户快速选择仓库、问题类型、目标符号、目标模块和分析范围。
- 如何让检索结果、证据片段、调用链、影响范围、修改建议更容易浏览和比对。
- 如何让错误日志分析、bug 定位过程更可视化、更可追踪。
- 如何让测试建议、测试生成、测试执行结果、覆盖率结果形成闭环界面。
- 如何让用户清楚看到“AI 当前知道什么、不确定什么、建议下一步做什么”。
- 如何把复杂流程拆成更自然的交互步骤，减少一次性信息轰炸。

UI 不应只是一个问答框。它应该逐步演进为一个面向“仓库理解、问题排查、修改规划、测试验证”的工程工作台。

## 6. 对后续 AI 的工作要求

后续 AI 在这个仓库中工作时，应默认遵循以下要求：

- 先理解现有架构，再做设计和实现，不要脱离当前项目演进方向随意发散。
- 优先增强对存量 C# 项目的理解、检索、分析、问答、定位、测试能力。
- 优先建设可复用的平台能力，而不是只做一次性脚本。
- 优先改善人机协作体验，包括 CLI、API、Web UI、工作流反馈和中间产物表达。
- 每次做功能时，都应思考这是否让“人和 AI 一起维护项目”变得更顺畅。
- 所有注释文档内容都使用中文。

## 7. 任务执行工作方式

后续 AI 每次开始执行任务时，除了响应用户当前输入，还必须先检查当前仓库中的待办文档，优先按待办推进工作。

默认检查以下文件：

- `D:\code\CodeBase\agentic_test_loop\Todolist.md`

如果存在大小写或命名差异，也应留意可能的等价文件，例如：

- `ToDoList.md`

执行规则如下：

1. 每次开始任务时，先检查 `Todolist.md` 是否存在待办事项。
2. 如果有待办任务，应优先逐个完成，而不是跳过待办去做无关扩展。
3. 每完成一个待办任务，都要同步更新待办状态。
4. 已完成的内容要整理写入 `Finished.md` 中，形成完成记录。
5. 每完成一项对项目有实质影响的工作，都要同步更新 `evolution.md`，记录项目能力、架构、工作流或交互上的进化。
6. 如果没有待办任务，则要根据当前软件已有功能、缺口和下一步产品方向，主动生成新的 `Todolist.md`。

在更新这些文档时，应遵循以下原则：

- `Todolist.md`：只保留当前未完成、正在进行、或新规划的事项。
- `Finished.md`：记录已经完成的任务、完成时间、结果摘要、相关说明。
- `evolution.md`：记录项目如何从旧状态演进到新状态，重点写“为什么演进”“新增了什么能力”“产品形态发生了什么变化”。

如果一次任务同时完成了代码改动、交互优化、测试补充、检索增强或 bug 修复，不要只改代码而忽略文档联动。文档更新本身是工作的一部分，不是可选步骤。

## 8. 一句话原则

这个项目要持续进化成一个面向存量 C# 工程的半自动化 AI 协作平台：

- 更懂仓库
- 更会解释
- 更能定位问题
- 更会补测试
- 更适合与人协作
- 更有持续进化的交互能力
